name: temporary N2 trifecta market foundation Mac validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Automatic Mac validation retired after immutable PASS evidence was recorded.'
